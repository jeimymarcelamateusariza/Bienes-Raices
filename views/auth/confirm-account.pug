extends ../layout/main

block contenido
    .py-10.min-h-screen.bg-primary
        h1.text-4xl.font-bold.text-white.mb-5.text-center Nidum
        p.text-xl.text-center.text-white.mb-5= pagina

        div(class="bg-white.shadow-md.rounded-lg.mx-auto.mt-10 p-5")
            p(class= error ? 'bg-red-400' : 'bg-green-300').py-2.px-5.rounded-lg.font-semibold.text-center.text-white= mensaje

            if !error
                a(href="/auth/login" class="text-secondary hover:text-secondary/90 block text-center mt-10 font-semibold") Inicia sesión